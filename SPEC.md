# SPEC — 项目宪法

> 本文档定义「小桌宠 Desktop Pet」应该成为什么、设计原则、边界与验收标准。
> 一般不变动；仅当技术栈无法满足需求、需更换路线时，经用户明确确认后修改。

## 1. 项目定位

一个面向 Windows 个人用户的**轻量桌面悬浮统计工具**：常驻系统托盘，在桌面上以小型悬浮窗实时展示今日键盘/鼠标操作量及系统资源占用，并支持按日回溯历史统计。

**一句话**：把"今天敲了多少字、点了多少下、电脑累不累"变成桌面上随时可见的一行数字。

## 2. 设计原则

### 2.1 EXE 优先（EXE-First）
- 打包后的 `dist/DesktopPet.exe` 是**唯一**用户交付物
- 所有功能必须通过 GUI 完成，用户永远不需要碰终端
- `python` / `pythonw` 仅作为开发调试手段

### 2.2 常驻而非退出
- 悬浮窗与设置窗的关闭事件一律拦截为隐藏（`closeEvent → hide`）
- 只有托盘菜单的"退出"才真正结束进程
- 退出/崩溃前必须落盘当前计数（`aboutToQuit` 兜底 flush）

### 2.3 数据安全
- 每日数据写 `data/YYYY-MM-DD.json`，**原子写入**（临时文件 + `os.replace`），崩溃不损坏历史
- 数据目录跟随程序目录（`utils.get_app_dir`），**不写入 C 盘用户目录**，随包带走
- 配置采用**深合并**：新版本新增配置键不破坏旧用户配置

### 2.4 尊重系统稳定
- 不修改内核、不 hook 系统级 API；仅使用公开稳定的接口
- 涉及系统级操作（开机启动注册表、点击穿透）均有明确开关且默认关闭/保守
- GPU 探测使用降级链：WMI → nvidia-smi → 0.0，不引入会产生控制台闪现的库（如 GPUtil）

## 3. 边界与约束

| 维度 | 约束 |
|---|---|
| 平台 | 仅 Windows（依赖 winreg、ctypes.windll） |
| UI | 仅 PySide6（Qt for Python） |
| 输入监听 | 仅 pynput 全局钩子（daemon 线程） |
| 数据 | 仅本地 JSON，无网络、无账号、无遥测 |
| 单实例 | 不强制（可多开） |
| 打包 | 仅 PyInstaller 单文件、无控制台 |

## 4. 验收标准

一个版本可称为"完成"，需同时满足：

- [ ] `python build.py` 成功产出 `dist/DesktopPet.exe`，双击可运行
- [ ] 悬浮窗实时刷新键盘/鼠标计数，每 10s 落盘，重启后数字延续
- [ ] CPU / 内存 / GPU / 磁盘统计正常显示；无独显环境 GPU 优雅降级为 0.0
- [ ] 关闭悬浮窗隐藏到托盘，托盘"退出"才结束进程
- [ ] 设置窗口所有开关、字号、布局、外观、位置项即时生效并持久化
- [ ] 日历 Tab 正确显示每日 K:/M: 统计，跨月可导航
- [ ] 打包 EXE 下「开机启动」勾选可写/删 HKCU Run 键
- [ ] 数据文件损坏或缺失时自动回退为 0，不崩溃
